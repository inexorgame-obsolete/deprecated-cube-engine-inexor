#! /usr/bin/env node

require('coffee-script/register');

# TODO: Dynamically select a port and tell the server!
# TODO: Use basic auth with a PSK over IPC
require("express-server").listen(48702, "localhost");
