0.3
fix rpc results (send valid JSON back) FIXED
embedd JS-console as an Angular-Application and set a default route to /
add a shortkey for F10 (Chrome-hide)
(ADDITIONALLY) add a browserify strategy

-> Merge with Inexor

0.4
add modularity (if we need it, should be simple though)
Add a tree-api for tree-gen and offer it via a RESTful API
-> remove CS from Inexor, yiha
